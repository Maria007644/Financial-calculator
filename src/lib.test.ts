import{describe,expect,it}from'vitest';import{calculateExpensesForMonth,calculateGoalProgress,calculateLessonRevenue,calculateRevenueForMonth,calculateRequiredLessonsForGoal,defaultState,generateLessonOccurrences}from'./lib';import type{Lesson}from'./types';
const lesson:Lesson={id:'1',title:'Группа',weekday:1,time:'10:00',type:'group',studentCount:4,duration:60,startDate:'2026-08-01',recurrence:'weekly',excludedDates:[],status:'planned'};
describe('финансы',()=>{it('считает цену группы за ученика',()=>expect(calculateLessonRevenue(lesson,defaultState().pricing)).toBe(3600));it('учитывает свою цену',()=>expect(calculateLessonRevenue({...lesson,customPrice:1000,customPricingMode:'perLesson'},defaultState().pricing)).toBe(1000));it('раскладывает годовые расходы',()=>{const s=defaultState();s.expenses=[{id:'e',title:'x',amount:60000,frequency:'annual',category:'x',enabled:true}];expect(calculateExpensesForMonth(s,new Date(2026,7,1))).toBe(5000)});it('считает прогресс и нужные уроки',()=>{expect(calculateGoalProgress(90000,120000)).toBe(.75);expect(calculateRequiredLessonsForGoal(30001,1500)).toBe(21)})});
describe('календарь',()=>{it('находит пять понедельников августа 2026',()=>expect(generateLessonOccurrences(lesson,new Date(2026,7,1),new Date(2026,7,31))).toHaveLength(5));it('учитывает исключение',()=>expect(generateLessonOccurrences({...lesson,excludedDates:['2026-08-03']},new Date(2026,7,1),new Date(2026,7,31))).toHaveLength(4));it('считает месяц по календарю',()=>{const s=defaultState();s.lessons=[lesson];expect(calculateRevenueForMonth(s,new Date(2026,7,1))).toBe(18000)})});
